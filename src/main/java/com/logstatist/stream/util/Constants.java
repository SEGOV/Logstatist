package com.logstatist.stream.util;

public class Constants {
    public interface LogLevel {
        String ERROR = "ERROR";
    }

    public interface FileExtension {
        String TXT = ".txt";
    }
}
